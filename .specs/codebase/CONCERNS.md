# Concerns (planejados — greenfield)

Semeado a partir da tabela de riscos da [INSIGHT.md §10](../../INSIGHT.md). A maioria destes riscos ainda não se materializou em código: F00/F01a/F01b entregaram só a superfície declarativa (sem compilador, bundler ou I/O). Um risco já concretizado e mitigado: "Nomes de classes no bundle" — `HttpStatus`/`HttpException` são god nodes (74 e 42 arestas no grafo, ver [STRUCTURE.md](STRUCTURE.md)), tornando `keepNames` na F10 ainda mais crítico do que o previsto originalmente (usados por 43 subclasses de exceção + todos os decorators HTTP/OpenAPI).

**F02 `poc-risks` validou 6 dos 7 riscos técnicos abaixo por experimento** (ver `.specs/features/poc-risks/findings/`) — nenhum foi refutado, mas quase todos ganharam requisitos concretos de implementação que não estavam óbvios antes do experimento. Linhas marcadas ✅ têm veredito; a tabela junta o resultado à mitigação original.

| Componente / Tema            | Risco                                                                                   | Mitigação planejada / Validado na F02                   |
| :--------------------------- | :-------------------------------------------------------------------------------------- | :---------------------------------------------------- |
| `compiler/slicer.ts`         | Bundles incorretos se o fatiamento remover código usado; herança e `this` dinâmico      | Fallbacks conservadores + warnings; testes de fixtures |
| `compiler/di-resolver.ts`    | Configurações não analisáveis aceitas silenciosamente                                   | Erros SAH com arquivo/linha                           |
| Source maps das fatias       | Stack traces apontando para código gerado                                               | ✅ **PASS** ([FINDINGS-206](../features/poc-risks/findings/FINDINGS-206.md)) — cadeia AST-transform→esbuild resolve à linha/coluna exata do TS original, inclusive minificado. Requisitos pro F06: o slicer precisa emitir seu próprio source map (nem `ts-morph` nem `tsc` fazem isso em transformação TS→TS — usar `magic-string`); preservar indentação ao mover método; preferir map inline; `sourcesContent: true`; teste de regressão automatizado (as duas formas de quebra são silenciosas). F07/F10 devem ligar `--enable-source-maps` via `NODE_OPTIONS`. |
| CloudFormation               | 500 recursos por stack (~80–90 rotas com uma Lambda por método)                          | Split de stacks; `granularity: controller`            |
| Nome de função Lambda        | Limite de 64 caracteres                                                                 | Chaves curtas + hash estável                          |
| Plugin (Serverless v3/osls)  | Loader pode não resolver `pkg/plugin`; schema pode não aceitar `nodejs24.x`; APIs removidas no osls 4; Serverless v3 sem manutenção | ✅ **PASS-COM-RESSALVA** ([FINDINGS-201](../features/poc-risks/findings/FINDINGS-201.md)) — loader resolve nos 3 motores (Serverless v3.40.0, osls 3.78.0/4.3.1); `serverless-offline` invoca nos 3. `nodejs24.x`: aceito de cara em osls 3.x/4.x, mas Serverless v3 tem o enum congelado em `nodejs20.x` (só warning em modo padrão, erro fatal em `configValidationMode: strict`) — **o plugin precisa patchear o schema em runtime** (não há `enum` direto em `provider.runtime`, é um `$ref`; tem que percorrer o schema procurando todo `enum` com `nodejs20.x`), condicional (no-op em osls). Nota separada: `serverless-offline` na lista de plugins faz `sls invoke local` travar indefinidamente nos 3 motores — documentar em TESTING.md. |
| ts-morph × TypeScript 7      | TS nativo sem a API JS do compilador                                                    | ✅ **Validado por protótipo (2026-09-16, F05 di-aot)** — `ts-morph@28.0.0` **não tem `peerDependency` em `typescript`** (só `@ts-morph/common`/`code-block-writer`); carrega sua própria cópia embutida do compilador, totalmente desacoplada da `devDependency typescript` do pacote (`6.0.3`). Protótipo confirmou, com uma fixture `@Module`/`@Injectable`/`@Inject` real: (1) resolução de tipo de parâmetro de construtor através de barrel (`export * from`) até a classe concreta; (2) leitura do array `providers`/`controllers` do decorator com resolução de cada identificador pro `ClassDeclaration` real; (3) detecção de dependência circular via DFS simples sobre o grafo resolvido; (4) ordenação topológica correta (folhas primeiro); (5) parâmetro de interface sem símbolo de classe (`InterfaceDeclaration`, não `ClassDeclaration`) — exatamente o gatilho que a REQ-036 exige pra exigir `@Inject(token)` explícito; (6) `@Inject(TOKEN)` no parâmetro resolvido corretamente ao `VariableDeclaration` do token; (7) `SpreadElement` num array de providers detectado e sinalizável como não-analisável. Risco rebaixado a **mitigado**, sem bloqueio pro Design de F05. |
| Schema Extractor             | Efeitos colaterais ao importar DTOs no build                                            | Regra documentada + warning                           |
| Modo B (padrão)              | SWC aumenta o tempo de build; `reflect-metadata` soma ao cold start                     | ✅ **PASS-COM-RESSALVA** ([FINDINGS-205](../features/poc-risks/findings/FINDINGS-205.md)) — `design:paramtypes` do SWC (decorators legado) é idêntico ao do `tsc` em 8 cenários (interface→`Object`, `@Inject`/`@Optional`, cross-file, forward-reference), legível por `reflect-metadata` sem adaptação. Duas ressalvas inofensivas ao metadata: SWC emite `design:type` extra (chave distinta, não colide); SWC não elide `import` de módulo usado só como tipo se faltar `import type` (arrasta módulo pro bundle — regra a formalizar em CONVENTIONS.md/lint pro F06). |
| Bundle ESM × dependência CJS | Handler ESM importando pacote publicado só em CJS pode falhar em runtime               | ✅ **PASS-COM-RESSALVA** ([FINDINGS-204](../features/poc-risks/findings/FINDINGS-204.md)) — não é `ERR_REQUIRE_ESM` (hipótese original refutada): com `packages: bundle` (default) falha com `Dynamic require of "X" is not supported` em builtins Node dentro de deps CJS transitivas — mitigado com banner `createRequire(import.meta.url)`, **deve ser default e não-sobrescrevível no F06**; com `packages: external`, falha com `Named export 'X' not found` (o `cjs-module-lexer` do Node não infere exports dinâmicos) — só a lib não resolve, requer `import default + destructuring` no handler (documentar em F10). Recomendação: `packages: bundle` como default do F06. |
| Matriz de modos A/B/C        | Triplica os cenários de teste do compilador                                             | Fixtures parametrizadas por tsconfig                  |
| Plugin do Vitest (testing)   | Reescrita de `Test.createTestingModule` falha com metadados dinâmicos                    | Exigir metadados analisáveis (SAH100); testes dedicados |
| Superfície pública           | ~150 exports na raiz; quebras acidentais de tipos                                       | Testes de tipo por área + snapshot do `.d.ts` público (já em produção — public-api-surface T-005) |
| `tsconfig.json` sem `noEmit`   | `tsc` executado sem `--noEmit` (ou tsconfig que estende o raiz) emite `.js`/`.d.ts` dentro de `src/` | Scripts usam `tsc --noEmit`; avaliar `noEmit: true` sem quebrar a geração de tipos do tsdown |
| Nomes de classes no bundle   | Minificação do esbuild sem `keepNames` altera `name` de exceções (e de classes usadas em mensagens/erros) | Bundler da F10 com `keepNames: true` + teste |
| INIT da Lambda               | Limite de 10s para `onModuleInit`/factories async                                       | Documentação                                          |
| Fluxo S3 de uploads          | Complexidade alta e mudança de contrato para o cliente                                  | Última fase; OpenAPI + helper de cliente              |
| Granularidade `method` × cold start | Handler-per-method pode não valer a complexidade sem ganho real de cold start medido | ✅ **PASS-COM-RESSALVA** ([FINDINGS-207](../features/poc-risks/findings/FINDINGS-207.md)) — sem número real de AWS ainda (deploy pendente de autorização explícita), mas o proxy local é claro em dois pontos: framework completo custa ~115 ms/900× de bundle vs. método isolado (~1 ms); e o valor de `method` sobre `controller` **não vem do roteamento** (~0,1 ms, irrelevante) — vem de isolar dependências entre métodos irmãos (~42 ms/497× quando métodos vizinhos importam `rxjs`/`express` que o método atual não usa). **Critério de aceite concreto para o F06:** o bundle do método X não pode conter módulo alcançável só a partir do método Y — senão o resultado converge pro custo de "por controller" e o esforço do slicer se perde. Nota operacional pro F10: granularidade `method` cresce rápido o nº de functions por serviço — vigiar o limite de 500 recursos por stack CloudFormation (linha já existente acima). |
